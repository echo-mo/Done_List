这个架构蓝图非常完美，我完全同意。

1. **目录结构** (api.js / store.js / ui.js / index.html) 职责划分清晰，符合“关注点分离”的最佳实践。
2. **store.isEditing 状态标志**的设计直接命中痛点，将状态从 DOM 剥离到 JS 内存中，是解决手机端闪退 Bug 的根本解法。

现在，我们正式开始拆分代码。为了保证代码质量并防止输出中断，**请严格按照以下步骤分批进行，不要一次性输出所有文件：**

**第一步：** 请先帮我生成底层的 `store.js` 和 `api.js` 的完整代码。
- 在 `store.js` 中，请实现所有的状态变量（如 tasks 缓存、_editingTaskId）和纯数据操作函数。
- 在 `api.js` 中，请实现封装好的 request 工具以及基于它的增删改查和轮询函数。

生成这两个文件后请停下来。等我确认无误，我们再进行第二步（生成最复杂的 ui.js 和精简后的 index.html）。开始吧！