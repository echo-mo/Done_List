# Context / 背景
edit按钮是一个用来修改任务内容的功能模块。delete按钮是一个用来删除任务的功能模块。请阅读项目中与“编辑任务”相关的代码文件。

# Bug Description / 缺陷描述
我在测试时发现一个后端 bug：
1. 当我使用edit按钮修改任务时，会增加一个新任务，而不是修改旧任务
2. 当我删除任务时，界面会在3秒左右之后自动刷新，任务会被恢复

# Expected Behavior / 期望行为
点击编辑并输入时，组件应该稳定保持在编辑状态，点击确定后任务被修改并且更新到数据库。
点击删除后，应该删除这一条数据，从数据库直接清除，防止下一次刷新任务被恢复。

# Task / 任务
请帮我找出导致该 bug 的根本原因，并提供修复后的代码。如果需要调整 CSS 布局（如避免软键盘遮挡）或修改事件监听逻辑，请一并给出优化建议。