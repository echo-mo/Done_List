这套初步方案非常清晰，解答你的三个确认问题如下：

1. **业务状态码规则**（1001~1004 + 5000）完全符合预期，语义明确，无需调整，就按这个标准执行。
2. **/health 接口**：同意保持纯文本。Zeabur 的健康检查探针只需要知道服务存活（200 OK）即可，返回 JSON 反而增加不必要的开销，且前端本就不会调用它。
3. **前后端适配**：必须前后端同步修改！考虑到当前项目没有任何全局异常兜底，如果只改后端，前端的逻辑会全部崩溃。
   - **具体要求**：在重构 server.js 的同时，请在前端帮我封装一个全局的 `request` 工具函数（替代散落各处的 `fetch`）。
   - **拦截逻辑**：前端在接收到响应后，需判断 `code !== 0` 时，统一抛出/弹窗显示 `message` 中的错误信息，彻底终结“静默失败”的问题。

确认以上原则后，请给出【第 1 点】和【第 2 点】相关的核心代码修改方案。

注意控制输出长度，完成这部分后，我们再进入【第 3 点：架构解耦与重构】。前端 index.html 超过 1000 行是目前的重灾区，后续我希望能优先剥离出独立的 JS 逻辑层和 API 请求层。