太棒了，第 1 点和第 2 点完成得非常扎实，彻底告别静默失败对项目的稳定性提升巨大。

现在我们进入第 3 点：架构解耦与重构。
**请务必先给我“重构前 vs 重构后”的架构对比方案，绝对不要直接开始大批量拆分和重写代码。**

在你的方案中，请包含以下内容：

1. **Mermaid 架构图补全**：
   请用 Mermaid 语法画出重构后的前端模块依赖图（Client-side Architecture）。让我直观地看到各层级是如何交互的。
   
   新增任务：绘制核心架构与数据流转图 (Architecture & Data Flowchart)

在我们进行后续的架构解耦（第 3 点）和文档编写（第 4 点）之前，我需要你先帮我梳理当前系统的运行脉络。请新增一个任务：

**任务要求：**
1. 请使用 **Mermaid 语法**，为我绘制一张当前项目的全局架构与数据流转图。
2. 图中需要清晰展现以下几个核心节点之间的交互流程与数据走向：
   - **前端层**：用户交互操作（如触发你之前提到的 Edit 按键等）与即将封装的全局 Request 请求拦截器。
   - **后端层**：Express 路由分发、API 业务处理。
   - **存储层**：MongoDB 生产环境与本地 storage.json 降级机制的分支逻辑。
3. 请在绘制解耦前的现状图后，顺便简述一下，在接下来的重构中，这张图的哪个部分会发生最大的结构性变化。
4. 后续做出的代码修改后，都需要更新这张图

请将这段 Mermaid 代码单独放在一个 Markdown 代码块中输出，以便我直接预览。确认你理解了这个新任务后，可以结合我们刚刚确认的【第 1 点】和【第 2 点】，开始输出你的代码修改和流程图了。

2. **前端拆分目录规划 (1050行 index.html 的解药)**：
   我希望前端能走向“关注点分离”。请评估以下拆分方向是否合理，并在你的方案中细化：
   - `api.js`：纯粹的网络请求层（包含刚才封装的 request 工具）。
   - `store.js` / `state.js`：纯粹的数据状态管理（内存中的 tasks 数组，只负责数据的增删改查）。
   - `ui.js` / `app.js`：DOM 操作与事件绑定层（渲染视图、监听点击）。
   - `index.html`：退化为纯粹的静态 DOM 骨架。

3. **核心痛点解决思路**：
   在 UI 交互层解耦时，请特别说明：我们将如何通过更好的焦点管理或状态隔离，来彻底解决我之前遇到的**“手机端软键盘弹起，输入框失去焦点导致 Edit 状态意外退出”**的 Bug？

请先输出上述 3 点的设计蓝图。等我确认目录结构和依赖关系没有问题后，你再一步步帮我提取代码。

