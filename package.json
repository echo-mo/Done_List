{
  "name": "done-list",
  "version": "5.0.0",
  "description": "Simple todo backend for saving tasks (used with a static frontend)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^7.1.0"
  }
}
